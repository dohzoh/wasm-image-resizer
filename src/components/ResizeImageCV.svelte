<script>
	let objectURL
	let img
	let canvas
	let ctx

	const postFilter = (blob) => {
		objectURL = URL.createObjectURL(blob)
	}

    (async () => {

    })()


</script>
<h2>src/components/ResizeImageCV.svelte</h2>